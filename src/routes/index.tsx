import { component$ } from "@builder.io/qwik";
export default component$(() => {
  return (
      <main class="h-full w-full bg-default-bg rounded-lg">

        <div class="bg-[#2e2e2e] w-[200px] h-[200px]"></div>

      </main>
  );
});